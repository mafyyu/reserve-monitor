services:
  reserve-monitor:
    build: .
    container_name: reserve-monitor
    env_file:
      - .env
    environment:
      - TZ=Asia/Tokyo
    volumes:
      - ./data:/app/data
    restart: unless-stopped
