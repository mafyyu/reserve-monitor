services:
  reserve-monitor:
    build: .
    container_name: reserve-monitor
    ports:
      - "3001:3001"
    env_file:
      - .env
    environment:
      - TZ=Asia/Tokyo
    volumes:
      - ./data:/app/data
    restart: unless-stopped
